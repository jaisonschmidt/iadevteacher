# Learner Workspace

This directory belongs primarily to the learner.

- `exercises/` — focused tasks for individual study steps
- `experiments/` — small explorations, debugging, and notes in code
- `projects/` — integrated module or final deliverables

Instructor defines where a task should live. Reviewer may inspect and test work here, but agents must not silently rewrite it.
